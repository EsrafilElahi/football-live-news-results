
function Title({ title }) {

    return (
        <div>
            <div className="alert alert-primary" role="alert">
                <span>{title}</span>
            </div>
        </div>
    )
}

export default Title
